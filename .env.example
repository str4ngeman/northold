# Atlas / hosted MongoDB. Allow the VPS IP in Network Access.
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/northold

# openssl rand -base64 48
AUTH_SECRET=change-me

# Created on first empty database only
ADMIN_EMAIL=admin@localhost
ADMIN_PASSWORD=change-me
