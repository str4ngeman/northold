services:
  app:
    image: ghcr.io/str4ngeman/northold:latest
    pull_policy: always
    restart: unless-stopped
    env_file: .env
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
